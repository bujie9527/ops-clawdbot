# Clawdbot Console - 环境变量示例
# 复制为 .env 并修改
# 数据库 URL 由 config 从 DB_* 自动生成，勿在此处写 DATABASE_URL

# 应用
APP_NAME=Clawdbot Console
DEBUG=false
APP_ENV=dev
APP_HOST=0.0.0.0
APP_PORT=8000

# 数据库（本地开发：docker 容器 ops_mysql，端口 3306）
# 务必与 infra/docker 中创建的库名一致：ops_console
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ops_console
DB_USER=root
DB_PASSWORD=rootpass

# 管理端登录（MVP 最简）
ADMIN_USER=admin
ADMIN_PASSWORD=changeme_admin_pass
SECRET_KEY=changeme_secret_key_for_session

# 节点鉴权（MVP 仅 project_a）
PROJECT_KEY_DEFAULT=project_a
NODE_TOKEN_PROJECT_A=changeme_node_token_project_a
